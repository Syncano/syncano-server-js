machine:
  node:
    version: 7

general:
  branches:
    ignore:
      - gh-pages
  artifacts:
    - "coverage/"

deployment:
  production:
    branch: master
    commands:
      - npm run test:coverage
      - npm run release
